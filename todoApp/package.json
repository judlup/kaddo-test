{
  "name": "todo-cli",
  "version": "0.1.0",
  "description": "A CLI TODO application — built to validate Kaddo's KDD workflow.",
  "bin": {
    "todo": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^20.14.0",
    "tsx": "^4.16.2",
    "typescript": "^5.5.2",
    "vitest": "^2.0.3"
  }
}
